/**
 * Clase base abstracta para servicios API que agrupan varios endpoints dentro de un bounded context.
 * Las subclases componen instancias de {@link BaseApiEndpoint} y delegan las operaciones HTTP.
 */
export abstract class BaseApi {
  // Sin métodos; las subclases componen endpoints concretos.
}
